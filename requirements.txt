# == Core Web Framework ==
fastapi==0.115.13
uvicorn[standard]==0.34.3

# == Data Validation & File Uploads ==
pydantic[email]==2.10.3
python-multipart==0.0.12

# == Database & ORM (PostgreSQL/Supabase) ==
sqlalchemy[asyncio]==2.0.36
asyncpg==0.30.0

# == Authentication & Security ==
PyJWT

# == AI & Machine Learning ==
# Both clients are used in the codebase:
# - google-generativeai → for `import google.generativeai as genai`
# - google-genai       → for `from google import genai`
google-generativeai==0.3.1
google-genai>=1.2.0,<2

# == PDF & Document Processing ==
python-docx==1.1.2
Pillow==11.0.0

# == Utilities ==
python-dotenv==1.0.1
httpx==0.25.2

# == Payment Processing ==
razorpay

# == Packaging runtime (needed by razorpay -> pkg_resources)
setuptools>=68.0.0