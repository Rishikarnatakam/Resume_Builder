MODERN RESUME TEMPLATE

IMPORTANT: Follow the template commands exactly as defined. Only modify them if specifically required by user request.

Key Commands (DO NOT CHANGE unless required):
- \geometry{margin=0.5in} for margins (can be changed when user requests)
- \name{\Huge name} for name with custom size (use \Large, \large, \normalsize, etc.)
- \projectentry{name}{dates}{tech}{description} for projects
- \item for bullet points
- \href{url}{text} for clickable links

IMPORTANT: Header information (\name) must be placed BEFORE \begin{document}, not after it.

Formatting Rules:
- Use template commands and environments exactly as defined
- Make URLs clickable with \href
- Escape % as \%
- Group skills into categories with clear label 
- Format projects as bullet points
- Only modify template commands when user explicitly requests changes
- Use maximum 3 links per line in the icons section for optimal layout
- When user requests margin changes, add \geometry{margin=X} after \documentclass
- When user requests name size changes, use \name{\Size Name} format
- Available name sizes: \tiny, \scriptsize, \footnotesize, \small, \normalsize, \large, \Large, \LARGE, \huge, \Huge
- Header commands (\name) go BEFORE \begin{document}
